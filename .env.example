# KvirNet 配置示例 —— 复制为 .env 后按需修改，.env 不会被提交（已加入 .gitignore）
# 留空或不设置则使用代码中的默认值。

# === 服务端 ===
# 对外宣告的 TCP 数据通道地址（公网IP:端口），会随 assign_ip 下发给客户端拨号
VIRNET_TCP_ADDR=43.138.247.132:8081
# 对外宣告的 WebSocket 信令地址（公网IP:端口 或 :端口）
VIRNET_WS_ADDR=:8080

# === 客户端（CLI / 桌面端通用） ===
# WebSocket 服务器地址，桌面端不经 flag.Parse，靠此项配置
VIRNET_SERVER=ws://43.138.247.132:8080/ws
